3단계: Flask-WTF 폼 처리
🎯 목표
HTML <form>에서 입력한 데이터를 Flask에서 받는 흐름 이해

Flask-WTF + WTForms 기본 문법 학습

유효성 검사(validator) 사용




해설
FlaskForm

Flask-WTF에서 제공하는 폼 기본 클래스

validators

DataRequired() → 값이 비어 있으면 오류

NumberRange(min, max) → 숫자 범위 제한

validate_on_submit()

요청이 POST이고, 모든 유효성 검사를 통과하면 True

CSRF 보호

폼 보안 토큰이 자동 생성 & 검증됨 (SECRET_KEY 필수)
